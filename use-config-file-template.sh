# To be written:
# A template for a script that reads a config file from a standard location.
# However, the location can be customised using an option to the command.
# The config file doesn't have to set all variables. If a variable is not set
# in the config file, then the script should use a default value.

echo "To be written." >&2
exit 1